<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">

  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to 'DevPoint Labs', root_path,
                                 class: "navbar-brand" %>
  </div>

  <!-- Top navigation menu -->
  <ul class="nav navbar-right top-nav">

    <!-- Announcements dropdown alerts-->
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-bell"></i> <b class="caret"></b></a>
      <ul class="dropdown-menu alert-dropdown">
        <li>
          <span class="notice" class="label label-success"><%= notice %></span>
        </li>
        <li>
          <span class="alert" class="label label-success"><%= alert %></span>
        </li>
        <li class="divider"></li>
        <li>
          <a href="#">View All</a>
        </li>
      </ul>
    </li>

    <!-- User drowpdown menu -->
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i> <%= current_user.first_name %> <b class="caret"></b></a>
      <ul class="dropdown-menu">
        <li>
          <% if current_user.staff? %>
            <%= link_to content_tag(:i, nil, :class=>"fa fa-fw fa-user") + ' Profile', root_path %>
            <%= link_to content_tag(:i, nil, :class=>"fa fa-fw fa-suitcase") + ' Admin', staff_base_admin_path %>
          <% else %>
            <%= link_to content_tag(:i, nil, :class=>"fa fa-fw fa-user") + '  Profile', dashboard_profile_path %>
          <% end %>
          <!-- <a href="#"><i class="fa fa-fw fa-user"></i> Profile</a> -->
        </li>
        <li>
          <%= link_to content_tag(:i, nil, :class=>"fa fa-fw fa-gear") + ' Settings', root_path %>
          <!-- <a href="#"><i class="fa fa-fw fa-gear"></i> Settings</a> -->
        </li>
        <li class="divider"></li>
        <li>
          <%= link_to content_tag(:i, nil, :class=>"fa fa-fw fa-sign-out") + ' Sign Out', destroy_user_session_path, method: :delete %>
          <!-- <a href="#"><i class="fa fa-fw fa-power-off"></i> Log Out</a> -->
        </li>
      </ul>
    </li>
  </ul>

  <!-- Render side navigation menu based on roles -->
  <% if current_user.staff? %>
    <%= render '/layouts/staff_nav' %>
  <% else %>
    <%= render '/layouts/student_nav' %>
  <% end %>
</nav>
