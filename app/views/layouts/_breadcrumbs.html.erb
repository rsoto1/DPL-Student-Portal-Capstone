<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">
        <div class='breadcrumb'>
            <%= build_breadcrumb(request.path) %>
        </div>
        <h1 class="page-header"><%= content_for?(:page_header) ? content_for(:page_header) : '' %></h1>
    </div>
  </div>

<!-- /.row -->
