<%= form_for([:dashboard, @assignment, @answer]) do |f| %>
<% if @answer.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@answer.errors.count, "error") %> prohibited
          this answer from being saved:</h2>
      <ul>
        <% @answer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Begin rendering the form -->
  <div class= 'form-group'>
    <%= f.label :title, 'Title:' %><br>
    <%= f.text_field :title, placeholder: 'Title', class: 'form-control input-lg' %>
  </div>
  <div class= 'form-group'>
    <%= f.label :body, 'Answer:' %><br>
    <%= f.text_area :body, placeholder: 'Answer', class: 'form-control input-lg' %>
  </div>  

  <%= f.submit 'Submit', class: 'btn btn-primary' %> 
  
  <%= link_to 'Back', dashboard_assignment_path(@assignment), class: 'btn btn-primary' %>
<% end %>

