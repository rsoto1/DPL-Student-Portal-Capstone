<%= form_for([:staff, @cohort, @repo]) do |f| %>
<% if @repo.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@repo.errors.count, "error") %> prohibited
          this repo from being saved:</h2>
      <ul>
        <% @repo.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Begin rendering the form -->
  <div class= 'form-group'>
    <%= f.label :name, 'Repo Name:' %><br>
    <%= f.text_field :name, placeholder: 'Repo Name', class: 'form-control input-lg' %>
  </div>
  <div class= 'form-group'>
    <%= f.label :link, 'Link:' %><br>
    <%= f.text_field :link, placeholder: 'Link', class: 'form-control input-lg' %>
  </div>  

  <%= f.submit 'Submit', class: 'btn btn-primary' %> 
  
  <%= link_to 'Back', staff_cohort_repos_path, class: 'btn btn-primary' %>
<% end %>