<h1>Location Listing</h1>
<p>This is just a temporary view. This will become the locations index page for staff.</p>
<p>Staff can come here to view a list of all current locations, add new ones, delete or edit current existing locations.</p>

<table class='table table-hover'>
  <thead>
    <tr>
      <th>Location Name</th>
      <th>Time Zone</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @locations.each do |location| %>
      <tr>
        <td><%= location.name %></td>
        <td><%= location.time_zone_name %></td>
        <td><%= link_to 'View', [:staff, location], class: 'btn btn-xs btn-default' %></td>
        <td><%= link_to 'Edit', edit_staff_location_path(location), class: 'btn btn-xs btn-default' %></td>
        <td><%= link_to 'Delete', [:staff, location], class: 'btn btn-xs btn-default', method: :delete, 
                                            data: { 
                                                    confirm: 'Are you sure you want to '\
                                                             'delete this location?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>