<%- page_header 'Add New Staff Member' %>
<%= form_for [:staff, @member], url: staff_members_path do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit 'Submit', class: 'btn btn-primary' %> 
  <%= link_to 'Back', staff_base_admin_path, class: 'btn btn-default' %>
<% end %>